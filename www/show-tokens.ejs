<!DOCTYPE html>
<html>

<head>
   <title>Scoped Tokens for Admins</title>
   <meta charset="utf-8">
   <link rel="stylesheet" type="text/css" href="styles.css">
</head>

<body>
   <div align=center>
      <div style="width:600px" align=left>

         <h1>Scoped Tokens for Admins</h1>
         <p>The OAuth flow was successfully completed:</p>
         <p>
            <ul>
               <li>Access token: <input type="text" size="30" id='accessToken' value=<%= accessToken %> />
                  &nbsp;&nbsp;<button onclick="copyAccessToken()"><i>copy</i></button></li>
               <li>Refresh token: <input type="text" size="30" id='refreshToken' value=<%= refreshToken %> />
                  &nbsp;&nbsp;<button onclick="copyRefreshToken()"><i>copy</i></button></li>
            </ul>
         </p>

         <p>Copy these values to a safe place, and close this page.</p>

      </div>
   </div>

   <script>
      function copyAccessToken() {
         let copyText = document.getElementById("accessToken");
         copyText.select();
         copyText.setSelectionRange(0, 99999); /*For mobile devices*/
         document.execCommand("copy");
      }

      function copyRefreshToken() {
         let copyText = document.getElementById("refreshToken");
         copyText.select();
         copyText.setSelectionRange(0, 99999); /*For mobile devices*/
         document.execCommand("copy");
      }
   </script>

</body>

</html>